name: Theme checks

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4.10"
          bundler-cache: true
      - name: Check JavaScript syntax
        run: node --check assets/js/main.js
      - name: Check Ruby syntax
        run: ruby -c lib/pwindows-theme.rb && ruby -c tools/verify-package.rb
      - name: Reject unresolved conflict markers
        run: '! grep -R -n -E "^(<<<<<<<|=======|>>>>>>>)" --exclude-dir=.git --exclude-dir=vendor .'
      - name: Build theme gem
        run: gem build pwindows-web-theme.gemspec
      - name: Verify packaged files
        run: ruby tools/verify-package.rb
      - name: Check whitespace
        run: git diff --check
